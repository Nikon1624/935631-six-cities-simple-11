export type CityType = {
  title: string;
  lat: number;
  lng: number;
  zoom: number;
};

export type PointType = {
  id: number;
  lat: number;
  lng: number;
};

export type PointsType = PointType[];
